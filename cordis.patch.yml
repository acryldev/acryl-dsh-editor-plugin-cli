# dsh-editor bundle patch.
#
# When installed through the DSH plugin CLI:
#
#   dsh plugin --profile <name> add acryl-dsh-editor-plugin@<version>
#
# the command appends `acryl-dsh-editor-plugin` to `dsh.profile.bundles`, and the
# profile boot merges THIS patch (one `insert` of the host-half row) exactly
# like a manual cordis.patch.yml mount line. No profile file edits needed.

- insert:
    - id: dsh-editor
      name: 'dsh-editor'
